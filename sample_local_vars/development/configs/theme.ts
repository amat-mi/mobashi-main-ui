export const CONFIG = {
    themeDefs: [ /** If needed, copy here and modify ThemeService.DEFAULT_THEME_DEFS */],
    brandColors: {
        lightColor: '#rrggbb',          //brand specific light color
        darkColor: ''                   //brand specific dark color (optional)
    }
};
